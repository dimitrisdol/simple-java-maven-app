pipeline {
	agent any 
	tools {
        maven 'Maven'
        jdk 'jdk'
    }
	stages{
		stage("Compile"){
			steps{
				sh 'mvn compile'
			}
		}
		stage("Test"){
			steps{
				sh 'mvn test'
			}
		}
		stage("Build"){
			steps{
				sh 'mvn clean install'
			}
		}
	}
}